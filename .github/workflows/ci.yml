name: CI

on:
  push:
    branches: [ main ]
    tags-ignore: [ '**' ]
  pull_request:

permissions:
  contents: read

jobs:
  ubuntu:
    uses: 9506hqwy/actions/.github/workflows/tauri-ci-ubuntu.yml@main

  windows:
    uses: 9506hqwy/actions/.github/workflows/tauri-ci-windows.yml@main
